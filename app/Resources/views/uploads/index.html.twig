{% extends 'base.html.twig' %}

{% block body %}
    <h1>uploads list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for upload in entities %}
            <tr>
                <td><a href="{{ path('uploads_show', { 'id': upload.id }) }}">{{ upload.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('uploads_show', { 'id': upload.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('uploads_edit', { 'id': upload.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('uploads_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
